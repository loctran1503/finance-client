import { ReactNode } from "react";

const NoneLayout = ({ children }: { children: ReactNode }) => {
    return (
      <>
        {children}
   
      </>
    );
  };
  
  export default NoneLayout;
  